[package]
name = "ui_tests"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]
# This is a standalone crate, not part of parent workspace

[dependencies]
serde = { version = "1.0", features = ["derive"] }
utoipa = "5.2"
http = "1.0"
axum = { version = "0.7", features = ["http1"] }

[lib]
path = "src/lib.rs"

