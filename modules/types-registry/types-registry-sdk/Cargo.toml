[package]
name = "types-registry-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "SDK for types-registry module: API trait, GTS entity types, and error definitions"

[lints]
workspace = true

[dependencies]
# Core dependencies for API trait
async-trait = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true, features = ["v5"] }
serde = { workspace = true }
serde_json = { workspace = true }

# GTS types from gts-rust submodule
gts = { path = "../gts-rust/gts" }

# Security context for API methods
modkit-security = { path = "../../../libs/modkit-security" }

[dev-dependencies]
tokio = { workspace = true }
